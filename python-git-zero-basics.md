# Python 与 Git：零基础第 1 周

这份计划只负责每天的 **4 小时计算机学习**。另外 4 小时继续按 [雅思计划](ielts-plan.md) 执行。

规则：每天只完成当天，不跳课，不追求记住全部内容。完成后回到 Codex 说“第 X 天完成了”，再进行下一天。

## 第 1 天：第一次运行 Python，第一次保存 Git 记录

今天目标：

- [ ] 运行一个 Python 程序
- [ ] 修改一行文字和一个数字
- [ ] 再次运行并看到变化
- [ ] 用 Git 保存这次修改
- [ ] 学习离散数学中的“命题”

### 第一步：打开终端并进入练习文件夹

在 Codex 下方的终端，复制并按回车：

```bash
cd "/Users/xinzhang/Documents/Codex/2026-08-25/github-plugin-github-openai-curated-remote/work/python-git-practice"
```

然后输入：

```bash
python3 daily_plan.py
```

应看到“今天的学习计划”和三项任务。看到就算第一步完成。

### 第二步：修改程序

在终端输入：

```bash
open -a TextEdit daily_plan.py
```

会打开一个文字编辑窗口。只修改两处：

1. 把 `"雅思写作"` 改成 `"雅思阅读"`。
2. 把 `"订单系统"` 这一行里的 `2` 改成 `3`。

按 **Command + S** 保存，然后回到终端再次输入：

```bash
python3 daily_plan.py
```

确认输出已经变成“雅思阅读”和“订单系统: 3 小时”。

### 第三步：再做一个很小的修改

在 TextEdit 中，把雅思阅读这一行的：

```python
"done": False
```

改成：

```python
"done": True
```

保存后再次运行：

```bash
python3 daily_plan.py
```

看到“雅思阅读: 2 小时（完成）”就对了。

### 第四步：用 Git 保存

依次输入，每输一行按一次回车：

```bash
git status
git add daily_plan.py
git commit -m "Complete Python day 1 exercise"
git log --oneline
```

如果 Git 提示没有姓名和邮箱，只需运行下面两行，再重复上面的 `git commit`：

```bash
git config --global user.name "你的英文姓名"
git config --global user.email "你的常用邮箱"
```

### 第五步：离散数学 1 小时

今天只理解“命题”：一句话如果只能判断为真或假，就是命题。

写在纸上并标记“是命题/不是命题”：

- 2 + 3 = 5
- 今天下雨吗？
- 请打开门。
- 所有正方形都有四条边。
- x + 1 = 3

答案：第 1、4 句是命题；第 2、3 句不是；第 5 句要知道 x 是什么才可以判断。

完成后告诉 Codex：**“第 1 天完成了，Git 输出是……”**，把终端最后几行复制过来即可。

## 第 2 天：变量、文字和数字

- 学习：变量就像给信息贴标签。
- Python：在 `daily_plan.py` 新增自己的姓名和当天日期，并让程序打印出来。
- Git：`git status → git add → git commit → git log --oneline`。
- 数学：集合、元素、并集和交集。
- 成果：程序可以显示“某人的某日学习计划”。

## 第 3 天：条件判断

- 学习：`if` 的意思是“如果……就……”。
- Python：当总学习时长少于 8 小时时，打印“还需要补充学习计划”；达到 8 小时时，打印“今日计划已满”。
- Git：学会看懂绿色的新增内容和红色的删除内容。
- 数学：逻辑中的“且”“或”“非”。
- 成果：程序能根据总时长给出提示。

## 第 4 天：列表与循环

- 学习：列表用来放很多项目；循环用来逐项处理。
- Python：新增 2 个自己的任务，让程序逐项打印全部任务。
- Git：学习 `git diff`，在提交前先看修改。
- 数学：真值表。
- 成果：输出至少 5 项任务和总时长。

## 第 5 天：函数

- 学习：函数是可重复使用的一小段工作。
- Python：把“计算总时长”和“打印任务”分别写成两个函数。
- Git：提交信息要写清楚本次做了什么。
- 数学：集合运算。
- 成果：代码更整齐，功能不变。

## 第 6 天：小作品——学习时间计算器

- Python：输入 3 个任务和时长，计算总时长；不足 8 小时给出差额。
- Git：创建一个新文件 `study_hours.py` 并提交。
- 数学：函数的输入与输出。
- 成果：独立运行的学习时间计算器。

## 第 7 天：复习与整理

- 重新运行第 1 天和第 6 天的程序。
- 用自己的话解释：变量、列表、循环、条件和函数。
- 查看 Git 历史：`git log --oneline`。
- 在 GitHub 计划仓库的 `ideas.md` 写下本周最难的三个点。
- 数学：复习命题、逻辑、集合和函数。
- 成果：完成第一周复盘，并准备开始订单系统代码整理。
